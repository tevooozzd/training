# Returns the size of iterable

string = ["Hey", "how", "are", "you", "?"]
int = [1, 2]
float = [1.1, 2.2, 3.3]

print(f"The size of string is {len(string)}, the size of int is {len(int)} and the size of float is {len(float)}")
