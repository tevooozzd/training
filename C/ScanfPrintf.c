#include <stdio.h>

// In this case i use the scanf and printf together to work on a more advanced model

int main() {
    int age;
    printf("Enter your age: ");
    scanf("%d", &age);  // Reads the user's age

    printf("Your age is: %d\n", age);  // Display age on screen
    return 0;
}
